# 🔧 サーバー起動ガイド

## エラーが発生した場合

### エラー: "Could not find a declaration file for module 'pdf-poppler'"

このエラーは修正済みです。以下の手順で再起動してください。

## 起動手順

### 方法1: 個別に起動（推奨）

**ターミナル1（バックエンドサーバー）:**
```powershell
npm run server
```

正常に起動すると、以下のメッセージが表示されます：
```
🚀 APIサーバーが起動しました: http://localhost:3000
```

**ターミナル2（フロントエンドサーバー）:**
```powershell
npm run web
```

正常に起動すると、以下のメッセージが表示されます：
```
  ➜  Local:   http://localhost:5173/
```

### 方法2: 同時に起動

```powershell
npm run dev:all
```

## 確認方法

1. **バックエンドサーバーの確認**
   - ブラウザで http://localhost:3000/api/health にアクセス
   - `{"status":"ok"}` が表示されればOK

2. **フロントエンドの確認**
   - ブラウザで http://localhost:5173 にアクセス
   - エラーメッセージが消えていればOK

## トラブルシューティング

### ポート3000が既に使用されている場合

```powershell
# ポート3000を使用しているプロセスを確認
netstat -ano | findstr :3000

# プロセスを終了（PIDを確認してから）
taskkill /PID <PID番号> /F
```

### サーバーが起動しない場合

1. エラーメッセージを確認
2. `.env`ファイルにAPIキーが設定されているか確認
3. `templates/transfer_template.pdf`が存在するか確認









